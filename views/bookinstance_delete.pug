extends layout

block content 
   h1=title 
   p2 Delete copy of:
   p 
      a(href=book.url) #{book.title}
   p Author: #{book.author.name}
   p Bi ID: #{id}
   p Do you want to delete this bookinstance, for real?
      form(method='POST' action='')
         div.form-group
            input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', required='true', value=id)
         button#btnDelete.btn.btn-danger(type='submit' name='btnDeleteBookInstance') Delete