extends layout 

block content 
   h1=title
   h2 Genre:  #{genre.name}

   if genre_books.length > 0
      p #[strong Delete the following book instances before attempting to delete this book.]

      ul
         for bi in genre_books
            li
               a(href=bi.url) #{bi.title} 
   else
      p Do you want to delete this book, for real??

      form(method= 'POST' action='')
         div.form-group
            input#genreid.form-control(type='hidden', name='genreid', required='true', value=genre._id)
            button#btnDelete.btn.btn-danger(type='submit' name='btnDeleteGenre') Delete